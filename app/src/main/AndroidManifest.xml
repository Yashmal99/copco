<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.copco">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true">
        <activity android:name=".splashactivity"
                   android:theme="@style/splashtheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
        <activity android:name=".MainActivity"
            android:theme="@style/splashtheme"></activity>
        <activity android:name=".copcomain"
            android:theme="@style/splashtheme"
            android:label="Copco"></activity>
        <activity android:name=".productioncard"></activity>
        <activity android:name=".plattingcard"></activity>
        <activity android:name=".aboutuscard"></activity>
        <activity android:name=".rawcard"></activity>
    </application>

</manifest>